import styled from 'styled-components';

export const Container = styled.div`
  width: 50px;
  height: 50px;
  background-color: red;
  input {
    width: 100%;
    height: 100%;
    svg {
      fill: var(--primary);
    }
  }
`;
